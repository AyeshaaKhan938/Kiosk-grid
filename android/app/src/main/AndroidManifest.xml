<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Permisos ─────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- USB Host — Reyeah control board serial -->
    <uses-feature android:name="android.hardware.usb.host" />

    <!-- BKX16/BKX18 camera SDK -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Mantener pantalla encendida (kiosk) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Auto-start cuando la tablet enciende tras corte de luz -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Kiosk hardening: keep the activity above the lock screen / on boot -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.SHOW_WHEN_LOCKED" />

    <!-- Remote APK self-updates: download via HTTP + trigger install intent -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Foreground service for the SerialMonitorService priority anchor.
         FOREGROUND_SERVICE_CONNECTED_DEVICE is the right type per Android 14+
         since the service exists to keep the UART connection healthy.
         (WAKE_LOCK is already declared earlier in this file.) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application
        android:label="VMFS USA"
        android:name=".VmfsApplication"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:keepScreenOn="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:windowSoftInputMode="adjustResize"
            android:alwaysRetainTaskState="true"
            android:clearTaskOnLaunch="false"
            android:stateNotNeeded="true">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Launcher principal + Home replacement (kiosk lockdown):
                 al registrar HOME + DEFAULT, Android puede elegir esta app
                 como pantalla principal. Pulsar el botón Home regresa al kiosk
                 en lugar de salir al launcher de fábrica. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- USB Device Attached: abre/activa la app al conectar el VMC -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!-- Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FileProvider: required so we can hand a freshly-downloaded APK
             to the system installer on Android 7+ (file:// URIs are blocked
             outside the app sandbox starting from API 24). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Foreground service that keeps the kiosk process priority high
             so Android's low-memory killer doesn't reap us. Notification
             stays under the immersive bar, customers never see it. -->
        <service
            android:name=".SerialMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- BKX16/BKX18 camera SDK background services -->
        <service android:name="com.bket.bketCamera.BketHostCameraService"
            android:exported="false" />
        <service android:name="com.bket.bketCamera.BketSubCameraService"
            android:exported="false" />
        <service android:name="com.bket.bketCamera.BketX18HostCameraService"
            android:exported="false" />
        <service android:name="com.bket.bketCamera.BketX18SubCameraService"
            android:exported="false" />

        <!-- BootReceiver: relanza MainActivity cuando la tablet enciende tras
             corte de luz / reinicio. Sin esto el kiosk se queda en el launcher
             de Android hasta que alguien toque el icono manualmente. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
